function expandCollaps(e) {
    $('.navbar-left').toggleClass('collapsed');
    $('.main-content').toggleClass('left-collapsed');
};

function expandCollapsRight(e) {
    $('.navbar-right').toggleClass('collapsed');
    $('.main-content').toggleClass('right-collapsed');
};